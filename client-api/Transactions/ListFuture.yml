info:
  name: ListFuture
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions/future"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
