info:
  name: DeleteTransaction
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/transactions/{{transactionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
