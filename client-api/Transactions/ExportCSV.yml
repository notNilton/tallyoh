info:
  name: ExportCSV
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions/export?from=2025-01-01&to=2025-01-31"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
