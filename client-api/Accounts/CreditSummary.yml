info:
  name: CreditSummary
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/accounts/credit-summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
