info:
  name: ListMembers
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/v1/accounts/{{accountId}}/members"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
