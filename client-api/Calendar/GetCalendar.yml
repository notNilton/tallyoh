info:
  name: GetCalendar
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/calendar?from=2025-01-01&to=2025-01-31"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
