info:
  name: GetStatement
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/cards/{{cardId}}/statement?from=2025-01-01&to=2025-01-31"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
