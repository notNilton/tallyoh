info:
  name: GetCard
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/cards/{{cardId}}"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
