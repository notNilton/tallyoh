info:
  name: CategoryBreakdown
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/dashboard/category-breakdown?month=2025-01&type=EXPENSE"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
