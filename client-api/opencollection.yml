opencollection: 1.0.0

info:
  name: Mirante
bundled: false
extensions:
  bruno:
    auth:
      mode: bearer
    auth:bearer:
      token: "{{accessToken}}"
    ignore:
      - node_modules
      - .git
