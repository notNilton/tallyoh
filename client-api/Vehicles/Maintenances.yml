info:
  name: Maintenances
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/vehicles/{{vehicleId}}/maintenances"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
