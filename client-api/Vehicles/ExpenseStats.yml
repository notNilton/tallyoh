info:
  name: ExpenseStats
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/vehicles/{{vehicleId}}/expenses-stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
